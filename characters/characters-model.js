const db = require('../data/db-config');

function find() {
  return null
};

function findById(id) {
  return null
};

function add(character) {
  return null
};

function update(id, update) {
  return null
};

function remove(id) {
  return null
};

module.exports = {
  find,
  findById,
  add,
  update,
  remove,

}